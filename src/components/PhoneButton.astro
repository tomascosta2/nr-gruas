---
export interface Props {
  variant?: 'header' | 'hero' | 'mobile' | 'footer';
}

const { variant = 'header' } = Astro.props;

const getClasses = () => {
  switch (variant) {
    case 'hero':
      return 'btn-lg btn-accent flex items-center gap-2 relative shadow-lg shadow-accent-500/20';
    case 'mobile':
      return 'btn-accent w-full mt-2 flex items-center justify-center gap-2';
    case 'footer':
      return 'btn-primary flex items-center gap-2';
    default:
      return 'btn-primary flex items-center gap-2';
  }
};

const phoneNumber = '2617777133';
const displayNumber = '(261) 777-7133';
---

<a 
  href={`tel:+54${phoneNumber}`} 
  class={`btn ${getClasses()}`}
  aria-label="Llamar ahora al servicio de grúas"
>
  {variant === 'hero' && (
    <span class="absolute -inset-0.5 rounded-lg bg-accent-500/50 animate-ping opacity-30"></span>
  )}
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5">
    <path fill-rule="evenodd" d="M1.5 4.5a3 3 0 013-3h1.372c.86 0 1.61.586 1.819 1.42l1.105 4.423a1.875 1.875 0 01-.694 1.955l-1.293.97c-.135.101-.164.249-.126.352a11.285 11.285 0 006.697 6.697c.103.038.25.009.352-.126l.97-1.293a1.875 1.875 0 011.955-.694l4.423 1.105c.834.209 1.42.959 1.42 1.82V19.5a3 3 0 01-3 3h-2.25C8.552 22.5 1.5 15.448 1.5 6.75V4.5z" clip-rule="evenodd" />
  </svg>
  <span class="relative">{variant === 'hero' ? 'LLAMAR AHORA' : displayNumber}</span>
</a>